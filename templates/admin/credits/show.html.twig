{% extends 'base.html.twig' %}

{% block title %}Credits{% endblock %}

{% block body %}
    <h1>Credits</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ credit.id }}</td>
            </tr>
            <tr>
                <th>Credit_amount</th>
                <td>{{ credit.creditAmount }}</td>
            </tr>
            <tr>
                <th>Credit_date</th>
                <td>{{ credit.creditDate ? credit.creditDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ credit.reason }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_credits_index') }}">back to list</a>

    <a href="{{ path('app_admin_credits_edit', {'id': credit.id}) }}">edit</a>

    {{ include('admin/credits/_delete_form.html.twig') }}
{% endblock %}
