{% extends 'base.html.twig' %}

{% block title %}Accounts{% endblock %}

{% block body %}
    <h1>Accounts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ account.id }}</td>
            </tr>
            <tr>
                <th>Balance</th>
                <td>{{ account.balance }}</td>
            </tr>
            <tr>
                <th>Opening_date</th>
                <td>{{ account.openingDate ? account.openingDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_accounts_index') }}">back to list</a>

    <a href="{{ path('app_admin_accounts_edit', {'id': account.id}) }}">edit</a>

    {{ include('admin/accounts/_delete_form.html.twig') }}
{% endblock %}
